function addAny(no: number) {
  return no + 2;
}

function getUpperValue(word: string){
  return word.toUpperCase();
}

getUpperValue("H3TvPateL")
console.log(addAny(5));

export {};

function isUser(name: string, email: string, isLoggedIn: Boolean) {
  console.log(`hiii everyone, my name is ${name}, my Email id is ${email}, `);
}

isUser("hetvi", "hetvi@techeniac.com", true);
