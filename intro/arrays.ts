const heros: string[] = [];
const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

heros.push("spiderman");
heroPower.push(12);

const users: User[] = [];

export {};
